import { withClasses } from "../../../hocs/with_classes";
import classes from "./schedule.module.scss";
import { Schedule as SC } from "./schedule";

export const Schedule = withClasses(classes, SC);
