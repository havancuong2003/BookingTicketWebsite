import { withClasses } from "../../../../hocs";
import classes from "./update-menu.module.scss";
import { UpdateMenu as UM } from "./update-menu";

export const UpdateMenu = withClasses(classes, UM);
